import { GetPermissionIdPipe } from './get-permission-id.pipe';

describe('GetPermissionIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPermissionIdPipe();
    expect(pipe).toBeTruthy();
  });
});

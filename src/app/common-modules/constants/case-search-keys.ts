export enum CaseBatchSearchKeys {
  fileType = 'Only .csv files are allowed',
  fileSize = 'The file size exceeds the limit allowed',
  allowedExtension = '.csv',
  CaseWithEntityIdExists = 'Entity already saved for case',
  fileUploadErrorCode = 500,
  personSearch = 'personnel',
  personEntityName = 'person'
}

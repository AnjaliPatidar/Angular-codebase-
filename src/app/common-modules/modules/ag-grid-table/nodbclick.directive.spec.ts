import { NodbclickDirective } from './nodbclick.directive';

describe('NodbclickDirective', () => {
  it('should create an instance', () => {
    const directive = new NodbclickDirective();
    expect(directive).toBeTruthy();
  });
});

import { GetValueByRefKeyPipe } from './get-value-by-ref-key.pipe';

describe('GetValueByRefKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueByRefKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
